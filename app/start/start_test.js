'use strict';

describe('myApp.start module', function() {

  beforeEach(module('myApp.start'));

  describe('start controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var startCtrl = $controller('StartCtrl');
      expect(startCtrl).toBeDefined();
    }));

  });
});